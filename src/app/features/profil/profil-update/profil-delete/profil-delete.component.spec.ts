/*
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ProfilDeleteComponent } from './profil-delete.component';

describe('ProfilDeleteComponent', () => {
  let component: ProfilDeleteComponent;
  let fixture: ComponentFixture<ProfilDeleteComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ProfilDeleteComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(ProfilDeleteComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
